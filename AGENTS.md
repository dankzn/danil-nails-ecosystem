# AGENTS.md

Работай с этим проектом как с продуктовой разработкой для запуска реальной CRM-экосистемы Danil Nails.

## Контекст проекта

Цель: создать систему для Danil Nails Studio, включающую сайт, клиентский кабинет, CRM, Telegram Mini App, Telegram bot, WhatsApp bot, базу клиентов, записи, расписание, уведомления, оплаты/предоплаты и аналитику.

Сначала продукт запускается для одного мастера в Москве. Архитектура должна поддерживать рост до салона и будущую географию в Испании.

Сроки:

- soft launch: около 2026-11-03;
- full launch: 2027-01-19 - 2027-02-19.

## Рабочий стиль

- Отвечай по-русски, если пользователь не просит иначе.
- Сначала проверяй существующие документы в `docs/`.
- Основной источник актуальных требований: `docs/05-requirements.md`.
- Не раздувай контекст: сохраняй решения в файлы, а не только в чат.
- Делай изменения маленькими законченными шагами.
- После значимых изменений обновляй roadmap или релевантный документ.
- Всегда мерджи готовую работу в `main`, не оставляй её висеть в отдельной ветке. Render деплоит только `main` (`render.yaml`), поэтому изменения, не смерженные в `main`, никогда не попадают на прод.
- Для пользовательских сценариев фиксируй: роль, цель, шаги, ошибки, состояние данных.
- Никогда не смешивай публичный клиентский статус лояльности и приватные внутренние метки. Приватные метки не должны попадать в клиентский кабинет, Telegram Mini App или публичные API.

## Технические предпочтения

Базовый стек, если не принято другое решение:

- TypeScript monorepo;
- Next.js для web/CRM/client cabinet;
- NestJS или Fastify для backend API;
- PostgreSQL + Prisma;
- Redis + BullMQ для очередей и уведомлений;
- grammY или Telegraf для Telegram bot;
- WhatsApp Cloud API напрямую для WhatsApp;
- Docker Compose для локальной разработки.

## Приоритет MVP

Первый запуск должен доказать запись клиента и управление расписанием:

- клиенты;
- услуги;
- мастера;
- рабочее время;
- свободные слоты;
- запись, перенос, отмена;
- CRM-календарь;
- Telegram Mini App или мобильный web booking;
- базовые уведомления.
